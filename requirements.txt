# 中国ETF双动量轮动策略 — Python依赖

# 数据获取
akshare>=1.18.0

# 数值计算
numpy>=1.24.0
pandas>=2.0.0

# 环境变量
python-dotenv>=1.0.0

# 可视化
matplotlib>=3.7.0

# HMM市场状态识别（可选，未安装时自动降级为波动率阈值法）
hmmlearn>=0.3.0
scikit-learn>=1.3.0

# DeepSeek LLM客户端（兼容OpenAI SDK，hybrid模式必需）
openai>=1.0.0

# (可选) Jupyter 分析
# jupyter>=1.0.0
# ipykernel>=6.0.0
# scikit-learn>=1.3.0
