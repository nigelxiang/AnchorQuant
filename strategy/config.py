"""
config.py — 策略全局配置
所有可调参数集中在此处，其他模块只读不写。
"""

# ─────────────────────────────────────────────
# 数据源选择
# ─────────────────────────────────────────────
# 'auto'     : 默认。Sina 失败则自动降级到 em
# 'baostock' : 直接用 BaoStock（注：BaoStock 不支持 ETF 品种，建议不用）
# 'akshare'  : 仅用 AkShare
DATA_SOURCE = 'sina'  # Sina资讯后端，无频率限制，配合_apply_split_correction实现前复权

# ─────────────────────────────────────────────
# ETF 标的池（全天候扩展版）
# ─────────────────────────────────────────────
ETF_POOL = {
    # ── 宽基（国内）──
    "510300": "沪深300ETF",
    "159915": "创业板ETF",
    "512100": "中证1000ETF",
    "510500": "中证500ETF",         # 中盘补充
    "588000": "科创50ETF",         # 科创板高成长

    # ── A股风格/行业 ──
    "510880": "红利ETF",            # 价值/高股息，风格切换基准
    "512880": "证券ETF",
    "159995": "芯片ETF",            # 半导体/科技核心
    "512170": "医疗ETF",            # 医疗健康
    "512660": "军工ETF",
    "159326": "电网设备ETF",        # 电力/新基建
    "159819": "人工智能ETF",        # AI主题
    "159206": "卫星互联ETF",        # 卫星通信/航天
    "512690": "酒ETF",
    "159755": "电池ETF",            # 储能/锂电
    "512120": "煤炭ETF",            # 能源周期
    "512800": "银行ETF",            # 价值防御
    "515050": "5GETF",              # 通信/科技
    "513010": "中概互联网ETF",      # 互联网板块
    "515700": "新能源车ETF",

    # ── 跨境QDII（全球分散）──
    "513100": "纳指100ETF",
    "513520": "日经225ETF",         # 日本股市
    "513180": "恒生科技ETF",        # 港股科技（港交所版）
    "513500": "标普500ETF",
    "513330": "恒生ETF",            # 港股宽基
    "159329": "沙特ETF",            # 中东/能源市场
    "159561": "德国ETF",            # 欧洲工业
    "513080": "法国CACETF",         # 欧洲消费/奢侈品
    "520870": "巴西ETF",            # 新兴市场/大宗商品

    # ── 跨境行业 ──
    "513120": "港股创新药ETF",
    "513310": "中韩半导体ETF",
    "159518": "标普石油天然气ETF",
    "159323": "港股通汽车ETF",
    "513090": "香港证券ETF",

    # ── 商品/对冲 ──
    "518880": "黄金ETF",            # 避险/通胀对冲

    # ── 防御/空仓替代 ──
    "511260": "十年国债ETF",
    "511880": "货币ETF银华日利",    # T+0，熔断首选
    "511010": "国债ETF",
}

# 防御资产（绝对动量基准线 & 熔断时持有）
# 货币ETF排首位：熔断时优先持有，收益稳定
DEFENSIVE_ETFS = ["511880", "511010", "511260"]

# ── 风格比值配对（用于A股特色宏观状态特征）──
# key=特征名, value=(分子ETF代码, 分母ETF代码)
STYLE_PAIRS = {
    "大小盘比": ("510300", "512100"),     # 沪深300 / 中证1000：大盘 vs 小盘
    "价值成长比": ("510880", "159915"),   # 红利 / 创业板：价值 vs 成长
}

# ── 跨境参考标的（用于全球风险偏好特征）──
CROSS_BORDER_REFS = ["513100", "513520", "513180", "159329", "159561", "518880"]

# ─────────────────────────────────────────────
# 回测区间
# ─────────────────────────────────────────────
START_DATE = "2015-01-01"   # 覆盖2015股灾后完整牛熊
END_DATE   = "2026-03-31"

# ─────────────────────────────────────────────
# 动量因子参数
# ─────────────────────────────────────────────
MOM_WINDOWS = [60, 90, 252]           # 季度/半年/年度动量窗口——去除20d短期噪声，加入90d季度趋势
MOM_WEIGHTS = [0.30, 0.20, 0.50]    # 年度50%+季度30%+半年20%，三周期互补
SKIP_DAYS   = 1                      # 跳过最近N日，避免短期反转污染
USE_RISK_ADJ_MOM = False             # 关闭风调动量
TREND_MA_WINDOW  = 0                 # 0=禁用趋势过滤（A/B测试证明无效）
RSI_OVERBOUGHT   = 85                # 85=仅过滤极端过热标的，保留主升段同时抑制追高回撤
CONSISTENCY_WEIGHT = 0.20            # 提高多周期同向趋势得分，削弱方向分裂的噪声信号

# ─────────────────────────────────────────────
# 组合构建参数
# ─────────────────────────────────────────────
TOP_N          = 5          # 持有动量最强的ETF数量（5只分散化优于4只集中）
HOLD_BUFFER    = 0          # 持仓缓冲：0=禁用（A/B测试证明有害）
REBAL_FREQ     = "2ME"      # 调仓频率：双月末 (Bimonthly)——减少噪声交易，显著提升夏普
ATR_WINDOW     = 14         # ATR计算窗口（风险平价仓位）
VOL_WINDOW     = 20         # 历史波动率窗口
TARGET_VOL     = 0.50       # 组合年化目标波动率（高TV+vol overlay = 允许高仓位+自动减仓保护）

# 相关性感知组合构建
CORR_FILTER_WINDOW = 60     # 计算相关矩阵的滚动窗口（日）
MAX_PAIR_CORR      = 0.82   # 最大允许持仓对相关系数（超过则用下一名候选替换低动量的）

# ─────────────────────────────────────────────
# 交易成本（单边）
# ─────────────────────────────────────────────
COMMISSION_RATE = 0.0003    # 佣金：万3（0.03%）
SLIPPAGE_RATE   = 0.001     # 滑点：0.1%（保守估算）
# 单边总成本 = 0.13%，双边 = 0.26%

# ─────────────────────────────────────────────
# 风控参数
# ─────────────────────────────────────────────
MAX_DD_TRIGGER     = 0.16   # 硬熔断触发阈值（最后一档：全防御）
CB_TIER1_DD        = 0.08   # 第一档：回撤≥8%，降低风险仓至70%
CB_TIER2_DD        = 0.12   # 第二档：回撤≥12%，降低风险仓至40%
# 分档熔断减少来回切换损耗：8%→30%防御，12%→60%防御，16%→100%防御
CB_TIMEOUT_MONTHS  = 6      # 熔断超时月数：分档重入（而非20月超时+全仓恢复），避免错过恢复行情
MAX_DD_HARD_LIMIT  = 0.25   # 策略硬红线（监控用）
SINGLE_POS_CAP     = 0.40   # 单只ETF最大仓位上限40%
TOTAL_INVEST_RATIO = 0.99   # 总仓位上限（配合vol overlay，几乎全仓以最大化alpha捕获）
RISK_RATIO         = 0.94   # 风险资产占比94%，保留6%常驻防御仓：略降进攻仓以进一步压缩双引擎回撤

# ─────────────────────────────────────────────
# 组合波动率控制（实时vol overlay）
# ─────────────────────────────────────────────
PORTFOLIO_VOL_CAP   = 0.12   # 月度调仓时组合级vol cap（收紧至12%进一步降低回撤）
WEEKLY_VOL_CAP      = 0.10   # 周中vol检查cap（略严于月度，优先拦截调仓间隙的急跌波动）
VOL_CHECK_FREQ      = 10     # vol检查频率（每10个交易日≈双周检查，减少过度干预）
VOL_LOOKBACK        = 30     # 月度vol计算回溯窗口（30日更稳定）
WEEKLY_VOL_LOOKBACK = 30     # 周中vol检测回溯窗口（30日与月度一致，更稳定）
VOL_SELL_TO_DEFENSE = True   # 周中减仓时将释放资金转入防御ETF（而非现金）

# 回撤速度熔断（独立于vol，直接检测净值急跌）
# 设为0=禁用（测试证明过于激进会损害收益）
DD_VELOCITY_DAYS     = 10    # 检测窗口（近N日组合收益率）
DD_VELOCITY_TRIGGER  = 0.0   # 0=禁用；若启用建议0.07（近10日跌>7%才触发）
# ─────────────────────────────────────────────
# 绩效基准
# ─────────────────────────────────────────────
BENCHMARK_ETF    = "510300"   # 沪深300ETF 作为基准
RISK_FREE_RATE   = 0.02       # 无风险利率：年化2%
TRADING_DAYS_PER_YEAR = 252

# ─────────────────────────────────────────────
# 信号模式
# ─────────────────────────────────────────────
# "quant"  : 纯量化规则（默认，历史回测使用，速度快）
# "hybrid" : 量化预处理 + DeepSeek LLM决策（实盘/--advice 时使用）
SIGNAL_MODE = "quant"

# ─────────────────────────────────────────────
# 纯量化引擎模式
# ─────────────────────────────────────────────
# "single" : 单一动量引擎（原始模式）
# "dual"   : 进攻子策略 + 防守子策略，并在月度层做策略切换
QUANT_ENGINE_MODE = "dual"

# 双引擎中的防守子策略池：
# 货币/国债负责防御底仓，黄金负责风险偏好极弱时的避险切换。
DUAL_ENGINE_DEFENSIVE_POOL = ["511880", "511010", "511260", "518880"]

# 双引擎月度切换规则：
# 1. 若沪深300跌破长期均线，则直接切到防守子策略
# 2. 或者当防守子策略代表资产的动量不弱于进攻子策略领先资产过多时，也切防守
DUAL_SWITCH_BENCHMARK = "510300"
DUAL_SWITCH_MA_WINDOW = 200
DUAL_SWITCH_SCORE_GAP = 0.01

# 双引擎防守连续持有保护：
# 进入防守引擎后，至少额外持续持有 N 个调仓周期；
# 同时可要求基准重新站稳长期均线后再允许切回进攻。
DUAL_DEFENSIVE_HOLD_MIN_REBALS = 0
DUAL_DEFENSIVE_REENTRY_BUFFER = 0.0

# 双引擎周级风险闸门：
# 仅在当前月度引擎为进攻时生效；触发后只做一次减风险并维持到下次月调仓，
# 避免周级信号来回切换带来的高交易成本。
DUAL_WEEKLY_GATE_ENABLED = True
DUAL_WEEKLY_GATE_MA_WINDOW = 120
DUAL_WEEKLY_GATE_DD_DAYS = 7
DUAL_WEEKLY_GATE_DD_TRIGGER = 0.025
DUAL_WEEKLY_GATE_SCALE = 0.90

# ─────────────────────────────────────────────
# LLM（DeepSeek）配置
# ─────────────────────────────────────────────
# Hybrid受限Overlay：量化主导，LLM只允许在量化基线附近小幅倾斜。
HYBRID_MAX_REL_TILT = 0.20          # 单资产相对量化基线的最大偏移比例（0.20 => 乘数范围 0.8~1.2）
HYBRID_ENABLE_DUAL_WEEKLY_GATE = True  # hybrid模式也沿用双引擎周级风险闸门，保持与量化基线一致的保护层

LLM_CONFIG = {
    "model":         "deepseek-chat",   # DeepSeek API 模型名
    "temperature":   0.1,               # 低温度=确定性输出（减少仓位随机性）
    "max_tokens":    512,               # 仓位JSON不需要太长
    "retry_count":   3,                 # JSON解析失败最大重试次数
    "timeout":       30,                # API单次请求超时（秒）
    "cache_enabled": True,              # 启用prompt hash缓存（相同市场状态不重复调用）
}

# ─────────────────────────────────────────────
# HMM 市场状态识别
# ─────────────────────────────────────────────
HMM_N_STATES   = 2     # 状态数：2=牛市(低波动上涨)/熊市(高波动下跌)
HMM_TRAIN_DAYS = 252   # 滚动训练窗口（1年，避免未来泄漏）

# ─────────────────────────────────────────────
# 数据缓存路径
# ─────────────────────────────────────────────
import os
BASE_DIR   = os.path.dirname(os.path.abspath(__file__))
DATA_DIR   = os.path.join(BASE_DIR, "..", "data", "raw")
OUTPUT_DIR = os.path.join(BASE_DIR, "..", "output")

for _d in [DATA_DIR, OUTPUT_DIR]:
    os.makedirs(_d, exist_ok=True)
